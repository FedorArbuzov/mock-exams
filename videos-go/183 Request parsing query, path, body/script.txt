Where do params live?

Query: r.URL.Query().Get("page"). Path vars depend on router — stdlib mux uses PathValue in 1.22+. Body: json.NewDecoder(r.Body).Decode(&req) with size limits.

Validate everything at the edge before domain logic. Bad input should never reach SQL.

Log request IDs, not full bodies with passwords.

Parse edge: query, path, JSON body — validate before business logic.

Master Go faster. Theory, hands-on labs, and interview questions — link in bio.
