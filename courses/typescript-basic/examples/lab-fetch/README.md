# Lab 30: typed fetch + Zod

Минимальный Shop API client к `http://localhost:8090`.

```bash
cd deploy/fastapi && docker compose up -d --build
cd courses/typescript-basic/examples/lab-fetch
npm install
npm start
```
