# Python Deep Dive — лабы

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -e ".[dev]"
pytest -v
```

| Модуль | Глава |
|--------|-------|
| `lab/decorators.py` | 06 |
| `lab/descriptors.py` | 11 |
| `lab/generators.py` | 07 |

Реализуйте TODO в файлах; эталон — `solutions/` (спойлер).
