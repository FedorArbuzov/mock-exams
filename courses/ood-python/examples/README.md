# OOD lab

```bash
pip install -e ".[dev]"
pytest -v
```

| Модуль | Глава |
|--------|-------|
| `ood/lru_cache.py` | 11 |
| `ood/rate_limiter.py` | 12 |
| `ood/parking_lot.py` | 10 |

Эталоны: `solutions/` (после своей попытки).
