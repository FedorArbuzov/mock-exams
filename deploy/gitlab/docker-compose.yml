# GitLab CE + Docker executor runner (локальная среда для курсов gitlab-*)
# Требования: Docker 20+, RAM 4+ GB для GitLab (лучше 6 GB)

services:
  gitlab:
    image: gitlab/gitlab-ce:latest
    container_name: mock-gitlab
    hostname: gitlab.local
    ports:
      - "8929:80"
      - "2222:22"
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://localhost:8929'
        gitlab_rails['gitlab_shell_ssh_port'] = 2222
        nginx['listen_port'] = 80
        gitlab_rails['time_zone'] = 'UTC'
        # Ускорение первого старта в учебной среде (не для production)
        prometheus_monitoring['enable'] = false
    volumes:
      - gitlab-config:/etc/gitlab
      - gitlab-logs:/var/log/gitlab
      - gitlab-data:/var/opt/gitlab
    shm_size: "256m"
    restart: unless-stopped

  gitlab-runner:
    image: gitlab/gitlab-runner:latest
    container_name: mock-gitlab-runner
    depends_on:
      - gitlab
    volumes:
      - gitlab-runner-config:/etc/gitlab-runner
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

volumes:
  gitlab-config:
  gitlab-logs:
  gitlab-data:
  gitlab-runner-config:
