# Single-node lab config (courses redis-basic / intermediate ACL, Streams)
bind 0.0.0.0
protected-mode no
port 6379
appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy allkeys-lru
save 60 1000
slowlog-log-slower-than 10000
slowlog-max-len 128
aclfile /usr/local/etc/redis/users.acl
