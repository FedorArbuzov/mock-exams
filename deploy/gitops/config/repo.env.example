# Скопируйте в repo.env и подставьте свой fork после push.
MOCK_GITOPS_REPO=https://github.com/FedorArbuzov/mock-exams.git
MOCK_GITOPS_REVISION=master

# В apps/*.yaml замените repoURL/targetRevision на те же значения (или используйте только bootstrap + direct apply).
