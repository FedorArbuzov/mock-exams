# Image prompts (optional hero frames)

## Control plane brain vs worker muscle
Isometric dark (#0B1020) Kubernetes diagram: glowing control plane stack (API server, etcd, scheduler, controllers) above three worker nodes running Pods. Cyan accent lines, premium SaaS style, no text, no people.

## Healthy control plane flow
Minimal motion-graphic still: kubectl apply arrow entering API server gate, etcd cylinder, controller loop, kubelet on worker node. Blue/cyan glow on dark background.

## Control plane failure split
Split-screen illustration: red dim control plane with blocked apply path on top; green worker nodes still running old Pods below. Educational infographic style, dark theme.
