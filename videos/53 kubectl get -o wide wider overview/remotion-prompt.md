Create a 9:16 vertical animated explainer video for Instagram Reels.

Topic: kubectl get -o wide

Style:
- Modern motion graphics, dark (#0B1020), cyan/green accents.
- TopicBanner on scene 1: "kubectl get -o wide"
- Brand footer: exallenge.tech
- Captions synced to narration; large phone-readable type.

Scene 1: Need node, IP, or extra fields fast?
Scene 2: kubectl get dash o wide. The default table is compact. Wide mode adds the columns you actually use while debugging - for Pods that often means node name, pod IP, and details the short view hides.
Scene 3: Why it helps: you see which node a Pod landed on without describe, spot IP issues, and correlate with node problems. For nodes, wide may show runtime and OS fields. Same get command, more signal.
Scene 4: What beginners get wrong: staring at default columns during an incident, then running ten describes. Wide is still not full YAML - use dash o yaml for that. Alias get pods dash o wide.
Scene 5: Rule to remember: when the table feels thin, add dash o wide before you dig deeper.
CTA: Master Kubernetes faster. Theory, hands-on labs, and interview questions - link in bio.
