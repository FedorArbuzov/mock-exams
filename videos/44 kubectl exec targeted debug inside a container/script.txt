How do you inspect an app from inside its own container?

kubectl exec. It runs a command inside a running container - perfect for quick diagnostics. kubectl exec dash it your-pod dash dash sh opens a shell. From there you can check environment variables, curl a dependency, resolve DNS, or look at a config file the app actually loaded.

This is how you answer real questions. Is the ConfigMap mounted where the app expects? Can this Pod reach the database Service by name? What does env show for the injected settings? You are testing from the app's exact network and filesystem view.

What beginners get wrong: expecting bash in a minimal image that only has sh, or no shell at all in a distroless image. Also treating exec changes as permanent - anything you edit inside a container vanishes on restart, because the image and manifest are the source of truth.

Practical tip: for images with no shell, use kubectl debug to attach an ephemeral container with tools.

Rule to remember: exec is for looking inside, not for making lasting changes.

Master Kubernetes faster. Theory, hands-on labs, and interview questions - link in bio.
