CPU is low but the queue is growing — how do I scale right?

In simple terms, use HPA on business metrics (for example, queue length).

Concretely, treat that as a testable rule: Use HPA on business metrics (for example, queue length). When reality disagrees, trust cluster signals over the story in your head.

Trust a few signals mapped to user pain: errors, latency, saturation, restarts, Ready replicas.

Alert only on pages you will actually act on — noise trains teams to ignore everything.

Beginner trap: forty dashboards and no trusted symptom.

If someone asks tomorrow what this changes, answer with the rule: use HPA on business metrics (for example, queue length).

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
