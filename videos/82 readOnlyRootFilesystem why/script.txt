Why make the root filesystem read-only?

In simple terms, it hardens against malware writes and forces explicit writable volumes for app needs.

Concretely, treat that as a testable rule: It hardens against malware writes and forces explicit writable volumes for app needs. When reality disagrees, trust cluster signals over the story in your head.

These fields are runtime levers for scheduling, restarts, eviction, and placement — not decorative YAML.

Change one field, watch Ready state and Events, and the concept becomes cause and effect.

Beginner trap: copying a full internet snippet without knowing the failure mode it prevents.

If someone asks tomorrow what this changes, answer with the rule: it hardens against malware writes and forces explicit writable volumes for app needs.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
