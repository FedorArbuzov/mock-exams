Pod keeps restarting forever — what first?

In simple terms, check `describe`, `logs --previous`, config/secrets, and the start command.

Concretely, treat that as a testable rule: Check `describe`, `logs --previous`, config/secrets, and the start command. When reality disagrees, trust cluster signals over the story in your head.

When it breaks, walk a short path: Pod status, Events, logs --previous, then the owner like a Deployment or Job. Random restarts are not a strategy.

Your job is to name the layer: image pull, config, crash, scheduling, or a controller recreating bad state on purpose.

Beginner trap: deleting Pods in a loop and calling it troubleshooting.

If someone asks tomorrow what this changes, answer with the rule: check `describe`, `logs --previous`, config/secrets, and the start command.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
