Create a 9:16 vertical animated explainer video for Instagram Reels.

Topic: kubectl get pods -A

Style:
- Modern motion graphics, dark (#0B1020), cyan/green accents.
- TopicBanner on scene 1: "kubectl get pods -A"
- Brand footer: exallenge.tech
- Captions synced to narration; large phone-readable type.

Scene 1: Can't find the Pod - maybe the wrong namespace?
Scene 2: kubectl get pods dash A, or dash dash all-namespaces. It lists Pods across every namespace in one table, with a NAMESPACE column so you can see where each Pod actually lives.
Scene 3: This is the classic morning mistake fix. Your context points at default, the app runs in staging or payments, and get pods looks empty. Dash A ends the guessing. Same flag works on deploy and svc too.
Scene 4: What beginners get wrong: running dash A on huge clusters without a filter. Or finding the namespace once, then forgetting to set it for the next commands. Tip: set-context namespace or pass dash n.
Scene 5: Rule to remember: empty get often means wrong namespace - try dash A first.
CTA: Master Kubernetes faster. Theory, hands-on labs, and interview questions - link in bio.
