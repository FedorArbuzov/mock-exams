After a restart, is data gone or still there?

In simple terms, without an external volume data is ephemeral; with a PVC it survives restarts.

Concretely, treat that as a testable rule: Without an external volume data is ephemeral; with a PVC it survives restarts. When reality disagrees, trust cluster signals over the story in your head.

This is how you move faster under pressure: name the layer, pick one proof, then change only what that proof supports.

If you skip the model, you will thrash — restarting things, editing YAML at random, and calling it debugging.

Beginner trap: knowing the buzzword while having no next check.

If someone asks tomorrow what this changes, answer with the rule: without an external volume data is ephemeral; with a PVC it survives restarts.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
