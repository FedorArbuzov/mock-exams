Why isn't the monitoring agent on every node?

In simple terms, tainted nodes often need matching tolerations in the DaemonSet.

Concretely, treat that as a testable rule: Tainted nodes often need matching tolerations in the DaemonSet. When reality disagrees, trust cluster signals over the story in your head.

Objects encode intent. A Deployment keeps N healthy copies. A Job runs to completion. Labels are how other objects find your Pods.

Before more YAML, ask what lifecycle you want: long-running, one-shot, per-node, or identity-stable — then choose the matching object.

Beginner trap: creating naked Pods and turning ops into babysitting.

If someone asks tomorrow what this changes, answer with the rule: tainted nodes often need matching tolerations in the DaemonSet.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
