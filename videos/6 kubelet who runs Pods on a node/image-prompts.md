# Image prompts (optional hero frames)

## kubelet as local agent
Close-up isometric worker node edge showing kubelet daemon hub connected to API server above and container runtime below, Pod in the center. Dark blue theme, cyan glow.

## Assignment to execution
Sequence still: scheduler assigns Pod to Node B, kubelet on Node B pulls image and starts container. Simple arrows, minimal labels, phone-readable layout.

## kubelet down NotReady
Worker node grayed out, kubelet icon off, NotReady badge, orphaned Pods disconnected from cluster view. Dramatic but clean infographic.
