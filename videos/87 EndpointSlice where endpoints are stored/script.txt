How does a Service know backend Pod IPs?

In simple terms, endpointSlice stores current backend Pod addresses for routing.

Concretely, treat that as a testable rule: EndpointSlice stores current backend Pod addresses for routing. When reality disagrees, trust cluster signals over the story in your head.

Most traffic outages are mismatches: selector, port, namespace, readiness, or Ingress rules with no controller.

Trace one request hop by hop — client, edge, Service, endpoints, Ready Pod — and stop at the first broken hop.

Beginner trap: blaming app code before confirming endpoints exist.

If someone asks tomorrow what this changes, answer with the rule: endpointSlice stores current backend Pod addresses for routing.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
