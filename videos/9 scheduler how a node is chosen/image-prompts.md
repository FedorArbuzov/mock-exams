# Image prompts (optional hero frames)

## Scheduler picks a node
Pending Pod above row of worker nodes, scheduler filter funnel removing bad nodes, scoring bars on survivors, one winner highlighted cyan.

## Assign vs run split
Two-step diagram: scheduler handoff arrow to node (assign only), kubelet on that node starting container. Clear role separation, dark infographic.

## Stuck Pending
Pod with Pending badge and spinning clock, all nodes blocked by taint/memory/nodeSelector icons. Events panel hint at bottom. Minimal text.
