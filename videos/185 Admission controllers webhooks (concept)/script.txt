Who can reject my YAML before it even runs?

In simple terms, the admission layer validates and mutates objects on create/update.

Concretely, treat that as a testable rule: The admission layer validates and mutates objects on create/update. When reality disagrees, trust cluster signals over the story in your head.

Security is blast-radius math: who can read this, who can change this, and what a stolen identity can do next.

Prefer dedicated ServiceAccounts, narrow Roles, and explicit NetworkPolicy over convenient shared defaults.

Beginner trap: cluster-admin culture and default ServiceAccount everywhere.

If someone asks tomorrow what this changes, answer with the rule: the admission layer validates and mutates objects on create/update.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
