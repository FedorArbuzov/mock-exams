Who applies manifests if not a human?

In simple terms, a GitOps agent syncs desired state from Git into the cluster.

Concretely, treat that as a testable rule: A GitOps agent syncs desired state from Git into the cluster. When reality disagrees, trust cluster signals over the story in your head.

Delivery quality is a bored repeatable path: pinned image, Git as source of truth, sync, smoke, rollback.

If humans must remember secret dashboard clicks, drift is already invited.

Beginner trap: kubectl edit in prod with no path back to Git.

For the video, huge YAML: replicas: 3.

If someone asks tomorrow what this changes, answer with the rule: a GitOps agent syncs desired state from Git into the cluster.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
