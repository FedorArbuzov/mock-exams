Which tag should CI put on an image?

In simple terms, use git sha/semver; never rely on `latest` for prod.

Concretely, treat that as a testable rule: Use git sha/semver; never rely on `latest` for prod. When reality disagrees, trust cluster signals over the story in your head.

Delivery quality is a bored repeatable path: pinned image, Git as source of truth, sync, smoke, rollback.

If humans must remember secret dashboard clicks, drift is already invited.

Beginner trap: kubectl edit in prod with no path back to Git.

If someone asks tomorrow what this changes, answer with the rule: use git sha/semver; never rely on `latest` for prod.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
