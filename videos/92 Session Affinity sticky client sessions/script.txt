How do I keep a client on the same Pod?

In simple terms, session Affinity sends repeat client requests to the same Pod.

Concretely, treat that as a testable rule: Session Affinity sends repeat client requests to the same Pod. When reality disagrees, trust cluster signals over the story in your head.

Most traffic outages are mismatches: selector, port, namespace, readiness, or Ingress rules with no controller.

Trace one request hop by hop — client, edge, Service, endpoints, Ready Pod — and stop at the first broken hop.

Beginner trap: blaming app code before confirming endpoints exist.

If someone asks tomorrow what this changes, answer with the rule: session Affinity sends repeat client requests to the same Pod.

Master Kubernetes faster. Theory, hands-on labs, and interview questions — link in bio.
