Terraform short 101: Separate states per env

TopicBanner: State design
Uses shared templates (Hook / Chips / Code / Contrast / Rule) + CTAScene.
Visual: {"mark": "STATE", "chips": ["dev", "stage", "prod"], "lines": ["dev.tfstate", "prod.tfstate"], "bad": "One state for all", "good": "Isolated environment state", "stamp": "SHORT RULE"}
