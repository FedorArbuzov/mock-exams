Terraform short 32: Endpoint overrides for local APIs

TopicBanner: Local Endpoints
Uses shared templates (Hook / Chips / Code / Contrast / Rule) + CTAScene.
Visual: {"mark": "where API calls go", "chips": ["endpoint", "local", "verify"], "lines": ["endpoints { ... }", "http://localhost", "$ terraform plan"], "bad": "hidden endpoint", "good": "explicit target", "stamp": "VERIFY THE ENDPOINT"}
