Terraform short 51: input variables basics

TopicBanner: variables
Uses shared templates (Hook / Chips / Code / Contrast / Rule) + CTAScene.
Visual: {"mark": "INPUT", "chips": ["type", "default", "var"], "lines": ["name = var.environment"], "bad": "Every setting", "good": "Clear interface", "stamp": "EXPLICIT INPUTS"}
