Terraform short 36: Mini checklist: safe provider setup

TopicBanner: Safe Providers
Uses shared templates (Hook / Chips / Code / Contrast / Rule) + CTAScene.
Visual: {"mark": "safe provider", "chips": ["version", "role", "region"], "lines": ["required_providers", "AWS_PROFILE=...", "$ terraform plan"], "bad": "hidden defaults", "good": "explicit setup", "stamp": "PROVIDER SAFETY CHECK"}
