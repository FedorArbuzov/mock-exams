Terraform short 25: provider aws region and defaults

TopicBanner: AWS Provider
Uses shared templates (Hook / Chips / Code / Contrast / Rule) + CTAScene.
Visual: {"mark": "which region?", "chips": ["provider", "variable", "profile"], "lines": ["region = \"us-east-1\"", "AWS_REGION=..."], "bad": "hidden default", "good": "explicit region", "stamp": "REGION IS A DECISION"}
