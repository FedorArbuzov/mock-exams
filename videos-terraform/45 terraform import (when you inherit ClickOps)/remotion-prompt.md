Terraform short 45: terraform import (when you inherit ClickOps)

TopicBanner: import
Uses shared templates (Hook / Chips / Code / Contrast / Rule) + CTAScene.
Visual: {"mark": "ADOPT", "chips": ["code", "import", "plan"], "lines": ["$ terraform import aws_s3_bucket.logs my-logs"], "bad": "State only", "good": "Code first", "stamp": "ADOPT SAFELY"}
