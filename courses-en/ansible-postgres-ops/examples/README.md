# ansible-postgres-ops examples

Type these into `~/nimbus-pg`. Do not copy the tree and call the course done.

```bash
ansible-galaxy collection install vitabaks.autobase:2.11.0 community.postgresql
```
