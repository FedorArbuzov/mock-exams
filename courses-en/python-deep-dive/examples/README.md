# Python Deep Dive — labs

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -e ".[dev]"
pytest -v
```

| Module | Chapter |
|--------|-------|
| `lab/decorators.py` | 06 |
| `lab/descriptors.py` | 11 |
| `lab/generators.py` | 07 |

Implement the TODOs in the files; the reference is in `solutions/` (spoiler).
